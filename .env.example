DATABASE_URL="postgresql://postgres:postgres@localhost:5433/pricengine_dev"
# Restricted role the app connects as at runtime (Row-Level Security is enforced
# for this role). DATABASE_URL above is the schema owner — migrations/seed only.
APP_DATABASE_URL="postgresql://pricengine_app:pricengine_app_pw@localhost:5433/pricengine_dev"
NEXTAUTH_SECRET="your-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
